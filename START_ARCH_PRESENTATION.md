# 🚀 Запуск презентации arch_slides.md

## Вариант 1: Из корневой директории (рекомендуется)

```bash
cd /home/pfile/presentation
npm run dev default/arch_slides.md
```

## Вариант 2: Из директории default

```bash
cd /home/pfile/presentation/default
npm run dev arch_slides.md
```

## После запуска:

1. Откройте браузер по указанному URL (обычно http://localhost:3030)
2. Перейдите на слайд с диаграммой Mermaid
3. **Наведите мышь на диаграмму** - должны появиться кнопки масштабирования
4. Откройте DevTools (F12) и проверьте консоль на наличие сообщений:
   - `Layout mounted, initializing mermaid zoom`
   - `Found X mermaid diagrams`

## Управление:

### Кнопки масштабирования (появляются при наведении):
- `+` - увеличить
- `⟲` - сбросить в 100%
- `−` - уменьшить
- `100%` - текущий масштаб

### Клавиатура:
- **Ctrl + колесико вверх** - увеличить
- **Ctrl + колесико вниз** - уменьшить
- Обычное колесико - прокрутка

## Если кнопок всё ещё нет:

1. Проверьте консоль браузера (F12) на ошибки
2. Убедитесь, что используется наша кастомная тема (в консоли должно быть логирование)
3. Попробуйте обновить страницу (F5)
4. Проверьте, что на слайде есть блок `.mermaid` через DevTools → Elements

## Альтернатива: если тема не подключается

Можно запустить обе презентации одновременно из корня:

```bash
# Основная презентация
npm run dev slides.md

# Или презентация arch
npm run dev default/arch_slides.md
```

