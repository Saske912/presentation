# Масштабирование и прокрутка диаграмм

## ✅ Что сделано:

### 1. **Увеличены размеры элементов диаграмм**
   - Размер текста: 13-15px (хорошо читается)
   - Увеличены отступы между элементами
   - Увеличены размеры блоков в sequence диаграммах

### 2. **Добавлена прокрутка для больших диаграмм**
   - Контейнер `.mermaid` имеет `max-height: 70vh`
   - Включена прокрутка: `overflow: auto`
   - Диаграммы могут быть больше экрана - просто прокручивайте внутри контейнера

### 3. **Интерактивное масштабирование диаграмм** ⭐ НОВОЕ!
   - **Кнопки управления**: При наведении на диаграмму появляются кнопки
     - `+` — увеличить масштаб
     - `⟲` — сбросить масштаб (100%)
     - `−` — уменьшить масштаб
   - **Ctrl + колесико мыши**: Зажмите Ctrl и крутите колесико для плавного масштабирования
   - **Диапазон**: от 30% до 300%
   - **Индикатор**: Текущий масштаб отображается между кнопками

### 4. **Красивый скроллбар**
   - Стилизованный скроллбар с градиентом
   - Ховер эффекты для лучшего UX

## 🎯 Как использовать:

### Прокрутка:
1. **Маленькие диаграммы** - отображаются полностью
2. **Большие диаграммы** - прокручивайте внутри контейнера
   - Используйте горизонтальную и вертикальную прокрутку

### Масштабирование (ZOOM):
1. **Наведите мышь** на диаграмму - появятся кнопки управления в правом верхнем углу
2. **Способы масштабирования**:
   - **Кнопки**: Нажимайте `+` для увеличения, `−` для уменьшения
   - **Ctrl + Scroll**: Зажмите Ctrl и крутите колесико мыши
   - **Сброс**: Кнопка `⟲` возвращает масштаб 100%
3. **После масштабирования** можно прокручивать диаграмму для просмотра разных частей

## 💡 Советы:

- 🔍 **Увеличьте диаграмму** (150-200%) для детального просмотра текста
- 🔎 **Уменьшите диаграмму** (50-80%) чтобы увидеть общую картину
- 📜 Используйте прокрутку для навигации по увеличенной диаграмме
- 🎯 Ctrl + Scroll даёт более плавное управление, чем кнопки
- ⟲ Быстро вернуться к исходному размеру - кнопка "сброс"

## ⚙️ Настройки (если нужно изменить):

### Размер текста в диаграммах
В `themes/custom/style.css`:
```css
.mermaid svg text {
  font-size: 14px !important;  /* Измените здесь */
}
```

### Максимальная высота диаграмм
В `themes/custom/style.css`:
```css
.mermaid {
  max-height: 70vh;  /* 70% высоты экрана */
}
```

### Диапазон масштабирования
В `setup/mermaid-zoom.ts`:
```typescript
const newLevel = Math.max(0.3, Math.min(3, currentLevel + delta))
//                       ^^^           ^
//                     минимум      максимум
// 0.3 = 30%, 3 = 300%
```

### Размеры элементов
В `setup/mermaid.ts`:
```typescript
flowchart: {
  padding: 15,
  nodeSpacing: 50,  /* Расстояние между узлами */
  rankSpacing: 50,  /* Расстояние между уровнями */
}
```

## 🚀 Запуск:

```bash
npm run dev
```

## 🎨 Особенности:

- ✨ **Плавные анимации** при масштабировании
- 🎯 **Умные контролы** - появляются только при наведении
- 🌈 **Градиентный дизайн** кнопок и скроллбара
- 📊 **Индикатор масштаба** показывает текущий уровень
- 🔄 **Сохранение состояния** каждой диаграммы независимо

## 🐛 Устранение проблем:

**Кнопки не появляются?**
- Убедитесь, что навели мышь на диаграмму
- Проверьте консоль браузера на ошибки

**Ctrl+Scroll не работает?**
- Попробуйте Command+Scroll на Mac
- Убедитесь, что курсор над диаграммой

**Диаграмма слишком маленькая?**
- Увеличьте масштаб до 150-200%
- Или измените базовый fontSize в настройках

